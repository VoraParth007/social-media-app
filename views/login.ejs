<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 min-h-screen flex items-center justify-center">

    <div class="bg-zinc-800 p-10 rounded-xl shadow-md w-full max-w-md text-white">
      <h3 class="text-3xl font-bold mb-6 text-center">Login to Your Account</h3>

      <form action="/login" method="POST" class="space-y-4">
        <input
          type="email"
          name="email"
          placeholder="Enter your email"
          class="w-full h-11 rounded-md bg-zinc-700 text-white px-4 outline-none"
          required
        />

        <input
          type="password"
          name="password"
          placeholder="Enter your password"
          class="w-full h-11 rounded-md bg-zinc-700 text-white px-4 outline-none"
          required
        />

        <input
          type="submit"
          value="Login"
          class="w-full h-11 rounded-md bg-blue-600 hover:bg-blue-700 transition text-white font-medium"
        />
      </form>

      <p class="text-center text-sm text-gray-400 mt-6">
        Don't have an account?
        <a href="/register" class="text-blue-400 hover:underline">Create Account</a>
      </p>
    </div>

  </body>
</html>
