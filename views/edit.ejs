<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class=" w-full flex justify-end ">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block"  href="/logout">Logout</a>
        </div>
        
     
      <h5 class="mb-5">Edit Your Post</h5>
      <form method="post" action="/update/<%= post._id %>">
        <textarea
          placeholder="Write your post here..."
          class="p-3 w-1/3 resize-none bg-transparent border-2 outline-none text-white h-40 mt-2 focus:border-blue-500 focus:ring-1 focus:ring-blue-500 border-zinc-800 rounded-md"
          name="content"
        ><%= post.content %></textarea>
        <input
          type="submit"
          class="px-3 w-40 py-2 text-sm block rounded-md bg-yellow-500 mt-2 text-black"
          value="Update Post"
        />
      </form>
      
    </div>
  </body>
</html>
